package com.palantir.giraffe.host;

/**
 * Indicates that implementations can authenticate connections to remote hosts
 * using information from a {@link Credential}.
 *
 * @author bkeyes
 */
public interface Authenticator {
    // marker interface
}
