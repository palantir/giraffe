ext.libVersions = [
    guava:   '27.0.1-jre',
    slf4j:   '1.7.9',
    jsr305:  '3.0.2',
    junit:   '4.12',
    mockito: '1.10.19',
    hamcrest: '1.3'
]

configurations.matching({ it.name in ['compile', 'runtime'] }).all {
    resolutionStrategy {
        failOnVersionConflict()
    }
}
